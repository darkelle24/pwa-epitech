import { BoxSettingsComponent } from './box-settings.component';
import { Route } from '@angular/router';

export const route: Route[] = [
    {
        path     : '',
        component: BoxSettingsComponent
    }
];
