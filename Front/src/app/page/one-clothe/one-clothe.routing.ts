import { Route } from '@angular/router';
import { OneClotheComponent } from './one-clothe.component';

export const route: Route[] = [
    {
        path     : '',
        component: OneClotheComponent
    }
];
