import { CurrentBoxDataComponent } from './current-box-data.component';
import { Route } from '@angular/router';

export const route: Route[] = [
    {
        path     : '',
        component: CurrentBoxDataComponent
    }
];
