import { LitegraphComponent } from './litegraph.component';
import { Route } from '@angular/router';

export const route: Route[] = [
    {
      path: '',
        component: LitegraphComponent
    }
];
